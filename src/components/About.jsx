import React from 'react';
import Base from './Base';

const About = () => {
    return (
        <Base id='about' title='About Me' content="My stack contains: Django, React JS, Tailwind CSS, Bootstrap, HTML, CSS, JavaScript."/>
    );
}

export default About;
